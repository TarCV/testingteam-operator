buildscript {
    ext.kotlin_version = '1.3.0'

    ext.tongs_version = new Properties().with {
        load(file('./../../plugin/gradle.properties').newReader())
        it
    }.get("VERSION_NAME")
    ext.testVersion = '1.3.0-rc02'
    ext.espressoVersion = testVersion.replaceFirst("^1", "3")


    repositories {
        mavenLocal()
        google()
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:3.3.2'
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "com.github.tarcv.tongs:tongs-gradle-plugin:$tongs_version"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

allprojects {
    repositories {
        mavenLocal()
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
