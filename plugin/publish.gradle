apply plugin: 'maven'
apply plugin: 'maven-publish' // TODO: consider removing one of these

project.afterEvaluate {
    publishing {
        publications {
            artifact(MavenPublication) {
                from components.java
            }
        }
    }
}